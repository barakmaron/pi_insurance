name: on-merge-workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
        with: 
          repository: barakmaron/pi-insurance-deploy
          token: ${{ secrets.ACCESS_TOKEN }}